import { beforeAll, afterAll, afterEach } from 'vitest';

// Add any missing global setups here

beforeAll(() => {
    // e.g. connect to a test database if needed
});

afterEach(() => {
    // e.g. reset db after each test
});

afterAll(() => {
    // e.g. close db connections
});
